# ============================================================================
# MongoDB Chatbot System - Environment Configuration Template
# ============================================================================
# Copy this file to .env and update the values according to your environment
# All REQUIRED variables must be set for the application to start
# Optional variables have default values that can be overridden

# ============================================================================
# REQUIRED: MongoDB Configuration
# ============================================================================
# MongoDB Atlas connection string with vector search capability
# Format: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
# Ensure your cluster has Atlas Vector Search enabled
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

# Database name for storing documents and embeddings (default: chatbot_db)
MONGODB_DATABASE=chatbot_db

# Collection name for document storage (default: documents)
MONGODB_COLLECTION=documents

# ============================================================================
# REQUIRED: OpenAI Configuration
# ============================================================================
# OpenAI API key - get from https://platform.openai.com/api-keys
# Must start with 'sk-' for API keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Embedding model for document vectorization (default: text-embedding-3-large)
# Options: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
EMBEDDING_MODEL=text-embedding-3-large

# Chat model for response generation (default: gpt-4)
# Options: gpt-4, gpt-4-turbo, gpt-3.5-turbo
CHAT_MODEL=gpt-4

# ============================================================================
# OPTIONAL: API Server Configuration
# ============================================================================
# Host address for the FastAPI server (default: 0.0.0.0)
# Use 127.0.0.1 for localhost only, 0.0.0.0 for all interfaces
API_HOST=0.0.0.0

# Port number for the FastAPI server (default: 8000)
API_PORT=8000

# Logging level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# OPTIONAL: Vector Search Configuration
# ============================================================================
# Name of the vector search index in MongoDB (default: vector_index)
VECTOR_INDEX_NAME=vector_index

# Similarity threshold for document retrieval (default: 0.7)
# Range: 0.0 to 1.0, higher values = more similar documents only
SIMILARITY_THRESHOLD=0.7

# Maximum number of documents to retrieve per query (default: 5)
# Range: 1 to 20, higher values = more context but slower responses
MAX_RETRIEVED_DOCS=5

# ============================================================================
# VALIDATION NOTES
# ============================================================================
# 1. MONGODB_URI: Must be a valid MongoDB connection string
# 2. OPENAI_API_KEY: Must start with 'sk-' and be a valid OpenAI API key
# 3. API_PORT: Must be a valid port number (1-65535)
# 4. LOG_LEVEL: Must be one of: DEBUG, INFO, WARNING, ERROR, CRITICAL
# 5. SIMILARITY_THRESHOLD: Must be a float between 0.0 and 1.0
# 6. MAX_RETRIEVED_DOCS: Must be a positive integer

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update MONGODB_URI with your MongoDB Atlas connection string
# 3. Update OPENAI_API_KEY with your OpenAI API key
# 4. Adjust optional settings as needed
# 5. Run the application: python main.py
# 6. Check logs for any configuration validation errors

# ============================================================================
# SECURITY NOTES
# ============================================================================
# - Never commit .env files to version control
# - Keep API keys secure and rotate them regularly
# - Use environment-specific configurations for different deployments
# - Consider using secrets management systems for production deployments